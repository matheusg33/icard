pagamento () {
    var method
    window.document.getElementById()

    switch {
        case {
            if (
                method = dinheiro;
            ) 
        }
        case {
            if (
                method = pix;
            )
        }
        case {
            if (
                method = debito;
            )
        }
        case {
            if (
                method = credito;
            )
        }

        }
    }
